문자열

  문장으로 된 자료형을 이야기하고 String으로 나타낸다.
  
  메소드
    string str;
    str.length();   //길이 반환
    if(str=="apple") //참이면 실행
    if(str.compare("banana") //위에꺼랑 똑같은 역할을 함
      //str.compare(str2) str이랑 str2랑 완전히 같으면 0반환
      //괄호안에 있는게 사전순으로 더 앞에 있는거면 양수, 괄호안이 더 뒤에있으면 음수 반환
    string strnum = "123";
    int num = stoi(strnum); //하면 num에는 123이 들어감
    string temp = to_string(num); //하면 temp에는 "123"이 들어감
    
